function sampleFunction() {
    let runNumber = 1; // initialisation
    let noOfRuns = 5;

    while (runNumber <= noOfRuns) { // condition
        console.log(`Hi! This is run number ${runNumber} trying to print Aakash`);
        runNumber = runNumber + 1; // increament
    }
}